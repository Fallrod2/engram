# AI orchestration docs — local only, never in the repo
CLAUDE.md
PROGRESS.md
DEV_OPUS.md
REVIEWER_SONNET.md
.claude/

# dependencies
node_modules/

# builds
dist/
build/

# test / coverage artifacts
coverage/
*.tsbuildinfo

# Playwright e2e artifacts
test-results/
playwright-report/
.playwright/

# local database (legacy sqlite, kept ignored — no effect)
data/

# Supabase local stack artifacts (config.toml stays tracked)
supabase/.branches
supabase/.temp

# env — never commit real secrets
.env
.env.*
!.env.example

# logs
*.log

# OS / editor cruft
.DS_Store

# Generated Vercel function bundle
api/app.bundle.mjs
